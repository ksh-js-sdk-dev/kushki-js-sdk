import { render } from "@testing-library/react";
import App from "./App.tsx";

describe("Tests on <App/> component", () => {
  test("renders Kushki Fields JS - DEMO", () => {
    render(<App />);
  });
});
